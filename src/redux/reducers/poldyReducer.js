const INITIAL_STATE = {
  isAuthenticated: false,
  tokenData: null,
};
export const poldyReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
