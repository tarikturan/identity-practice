const INITIAL_STATE = {
  isAuthenticated: false,
  tokenData: null,
};

export const companyReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
