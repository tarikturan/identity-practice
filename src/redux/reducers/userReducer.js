const INITIAL_STATE = {
  isAuthenticated: false,
  tokenData: null,
};
export const userReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
